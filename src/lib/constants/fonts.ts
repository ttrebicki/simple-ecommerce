import { Inter } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin', 'latin-ext'],
  weight: 'variable',
});

export const interUrl =
  'https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap';
